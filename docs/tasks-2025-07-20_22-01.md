# Task List: Remove Schedule.Name Field (2025-07-20_22-01)

## Phase 1: Analysis and Discovery
[x] Locate and examine `Schedule` class definition in `src/StudioScheduler.Core/Models/Schedule.cs`
[x] Search for all `Schedule.Name` usages across the entire solution using regex search
[x] Identify all DTOs in `src/StudioScheduler.Shared/Dtos/` that contain `Schedule.Name` references
[x] Document all findings in detailed analysis report

## Phase 2: Database Layer Updates
[x] Remove `Name` property from `Schedule` model in `src/StudioScheduler.Core/Models/Schedule.cs`
[x] Remove `Name` column mapping from `ApplicationDbContext.cs` in `src/StudioScheduler.Infrastructure/Data/`
[x] Create new EF Core migration to drop `Name` column from `Schedules` table
[x] Review and update any seed data files that reference `Schedule.Name`
[x] Update `ApplicationDbContextModelSnapshot.cs` if needed

## Phase 3: Backend Logic Refactor
[x] Remove `Schedule.Name` references from all repositories in `src/StudioScheduler.Infrastructure/Repositories/`
[x] Update all commands in `src/StudioScheduler.Core/Commands/Schedules/` to remove `Name` field handling
[x] Update all queries in `src/StudioScheduler.Core/Queries/Schedules/` to remove `Name` field returns
[x] Refactor `ScheduleValidator.cs` in `src/StudioScheduler.Core/Validators/` to remove `Name` validation rules
[x] Update any services in `src/StudioScheduler.Core/Services/` that reference `Schedule.Name`

## Phase 4: API Layer Updates
[x] Remove `Name` from Schedule DTOs in `src/StudioScheduler.Shared/Dtos/`
[x] Update API controllers in `src/StudioScheduler.Server/Controllers/` to remove `Name` field handling
[x] Update request/response models that include `Schedule.Name`
[x] Update API documentation/swagger annotations if present

## Phase 5: Frontend Refactor
[x] Search and identify all `Schedule.Name` references in Blazor components in `src/StudioScheduler.Client/Components/`
[x] Search and identify all `Schedule.Name` references in Blazor pages in `src/StudioScheduler.Client/Pages/`
[x] Replace UI references with `DanceClass.Name` in `ScheduleDetailsModal.razor`
[x] Replace UI references with `DanceClass.Name` in `ScheduleEditMode.razor`
[x] Replace UI references with `DanceClass.Name` in `ScheduleViewMode.razor`
[x] Update `Schedule.razor` page if it displays `Schedule.Name`
[x] Update any services in `src/StudioScheduler.Client/Services/` that reference `Schedule.Name`

## Phase 6: Testing Updates
[x] Update unit tests in `tests/StudioScheduler.UnitTests/` that reference `Schedule.Name`
[x] Update integration tests in `tests/StudioScheduler.IntegrationTests/` that reference `Schedule.Name`
[x] Update any Playwright tests in `tests/StudioScheduler.PlaywrightTests/` that reference `Schedule.Name`
[x] Run all tests to ensure no breaking changes

## Phase 7: Documentation and Cleanup
[x] Update any documentation files that reference `Schedule.Name`
[x] Create migration notes for database schema changes
[x] Review all changes against KISS, YAGNI, SRP, DRY principles
[x] Perform final solution-wide search to ensure no lingering `Schedule.Name` references
[x] Request user review and approval before final deployment
